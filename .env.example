# Firebase sync proxy endpoint (required)
VITE_FIREBASE_PROXY_URL=https://sync-cookie-proxy-xxxx.a.run.app

# Optional legacy OAuth settings (if GitHub flow is enabled in your branch)
# VITE_GITHUB_CLIENT_ID=your_github_oauth_app_client_id
# VITE_GITHUB_OAUTH_PROXY_URL=https://your-backend.example.com/oauth/github/exchange
